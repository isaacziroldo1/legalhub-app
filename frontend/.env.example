# URL interna do Fastify (somente servidor Next — Route Handlers / BFF)
API_INTERNAL_URL=http://localhost:3001/api

# Nome opcional do cookie HttpOnly de sessão (padrão: legalhub_token)
# SESSION_COOKIE_NAME=legalhub_token
